services:
  - type: web
    name: smell-reg
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
    disk:
      name: smell-reg-data
      mountPath: /app/data
      sizeGB: 1
