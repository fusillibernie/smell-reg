tasks:
  - init: pip install -e ".[dev]" && playwright install --with-deps chromium
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000

ports:
  - port: 8000
    onOpen: open-browser
    visibility: public
